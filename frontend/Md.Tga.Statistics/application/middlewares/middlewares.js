const baseMiddleware = require('./base-middleware');
const pugMiddleware = require('./pug-middleware');
const statisticsMiddleware = require('./statistics-middleware');

module.exports = {
  baseMiddleware,
  pugMiddleware,
  statisticsMiddleware,
};

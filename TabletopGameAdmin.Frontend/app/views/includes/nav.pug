nav
  input(type='checkbox')
  .menu-symbol
    div
    div
    div
  .nav
    ul
      each entry in pugData.menu
        li(class=entry.class)
          if entry.link
            a.hover-underline-animation(href=entry.link)= entry.name
          else
            a.hover-underline-animation= entry.name
  